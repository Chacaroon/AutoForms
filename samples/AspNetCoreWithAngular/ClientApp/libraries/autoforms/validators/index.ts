import { required } from "./required.validator";
import { minLength } from "./min-length.validator";
import { maxLength } from "./max-length.validator";

export const AfValidators = {
    required,
    minLength,
    maxLength
}
